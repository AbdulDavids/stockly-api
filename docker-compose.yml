version: '3.8'

services:
  stockly-api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      # Configure yfinance cache to use writable directory
      - YFINANCE_CACHE_DIR=/tmp/yfinance_cache
    volumes:
      # Mount a writable cache directory
      - yfinance_cache:/tmp/yfinance_cache
    restart: unless-stopped

volumes:
  yfinance_cache: