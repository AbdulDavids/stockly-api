# yfinance Configuration
# ======================

# Rate limiting: requests per second to Yahoo Finance API
# Lower values = more conservative, higher values = faster but may hit rate limits
YFINANCE_RATE_LIMIT=1.0

# Cache TTL: how long to cache responses (in seconds)
# 300 = 5 minutes (good for real-time data)
# 900 = 15 minutes (good for less time-sensitive data)
YFINANCE_CACHE_TTL=300

# Cache directory: where yfinance stores timezone cache files
# Use /tmp for temporary storage, or /app/cache for persistent storage
YFINANCE_CACHE_DIR=/tmp

# Verbose logging: show cache hits/misses and rate limiting events
# Set to 'true' for debugging, 'false' for production
YFINANCE_VERBOSE=false

# Example configurations for different environments:
# =================================================

# Development (verbose logging, fast responses)
# YFINANCE_RATE_LIMIT=2.0
# YFINANCE_CACHE_TTL=60
# YFINANCE_VERBOSE=true

# Production (conservative, longer cache)
# YFINANCE_RATE_LIMIT=0.5
# YFINANCE_CACHE_TTL=600
# YFINANCE_VERBOSE=false

# High-traffic (very conservative)
# YFINANCE_RATE_LIMIT=0.25
# YFINANCE_CACHE_TTL=900
# YFINANCE_VERBOSE=false